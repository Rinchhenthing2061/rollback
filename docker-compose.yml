version: "3.9"

services:
  frontend:
    build:
      context: .
      target: production
      dockerfile: Dockerfile
    container_name: chatboq-frontend
    ports:
      - "3003:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production